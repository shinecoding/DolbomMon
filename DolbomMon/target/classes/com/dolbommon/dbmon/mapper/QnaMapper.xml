<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qnaMapper">
	<select id ="QnaList" resultType="com.dolbommon.dbmon.QnA.QnAVO">
		select seq, subject, userid, writedate from qna
		order by seq desc where type=1
	</select>
<!-- 	
	<select id="qnaBoardSelect" parameterType="int" resultType="com.dolbommon.dbmon.QnA.QnAVO">
		select no, subject, content, userid, writedate
		from qna where seq=#{param1}
	</select> -->
</mapper> 
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->
		
	<!-- 관리자 로그인 인터셉터 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/management"/>
			<bean class="com.dolbommon.dbmon.Interceptor.ManagerLoginInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>
	
	<!-- 자동 로그인 인터셉터 -->
	<mvc:interceptors>
	    <mvc:interceptor>
			<mvc:mapping path="/"/>
			<bean class="com.dolbommon.dbmon.Interceptor.AutoLoginInterceptor"/>
	    </mvc:interceptor>
	</mvc:interceptors>
	
	<!-- 로그인 인터셉터 -->
	<mvc:interceptors>
	 	<mvc:interceptor>
	 		<mvc:mapping path="/parentView"/>
	 		<mvc:mapping path="/teacherView"/>
	 		<mvc:mapping path="/identityForm"/>
	 		<bean class="com.dolbommon.dbmon.Interceptor.LoginInterceptor"/>
	 	</mvc:interceptor> 
	 </mvc:interceptors>
	
</beans>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dolbommon.dbmon.chat.ChatDaoImp">
	<insert id="insertRoom" parameterType="Room">
		insert into chatroom (roomseq, userid, rommname, indate) values(chatseq.nextval,${userid},#{roomname},sysdate)
	</insert>
	<select id="selectAllRoom" parameterType="Room">
		select roomseq, userid, roomname, indate from chatroom order by indate desc
	</select>
	
</mapper>